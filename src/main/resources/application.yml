# ============================================================
# Spring Boot 4.1 后端配置
# 生活习惯助手 - 后端业务服务（习惯 / 目标 / 打卡 / 提醒 / 统计）
# ============================================================

spring:
  profiles:
    active: local
  application:
    name: habit-agent

logging:
  level:
    root: INFO
    com.habit.agent: DEBUG

# ===== 本地开发环境（激活时见 application-local.yml 覆盖）=====
---
spring:
  config:
    activate:
      on-profile: local
  # 本地开发使用 application-local.yml 中的 datasource 配置
